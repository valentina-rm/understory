source "https://rubygems.org"

# GitHub Pages builds the site for you on push. This gem pins the exact
# versions GitHub Pages uses, so a local preview matches production.
gem "github-pages", group: :jekyll_plugins

# Local preview only (not used by GitHub Pages):
#   bundle install
#   bundle exec jekyll serve --port 4001
gem "webrick", "~> 1.8"
